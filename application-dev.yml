spring:
  config:
    activate:
      on-profile: dev

  datasource:
    url: jdbc:h2:mem:java
    driverClassName: org.h2.Driver
    username: sa
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  flyway:
    enabled: true
